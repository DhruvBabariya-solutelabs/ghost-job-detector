import { InstallPage } from '@/components/InstallPage';

export const metadata = {
  title: 'Install the extension · Ghost Job Detector',
};

export default function InstallRoutePage() {
  return (
    <main>
      <InstallPage />
    </main>
  );
}
