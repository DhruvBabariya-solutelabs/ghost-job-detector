import { Dashboard } from '@/components/Dashboard';

export const metadata = {
  title: 'Your analyses · Ghost Job Detector',
};

export default function DashboardPage() {
  return (
    <main>
      <Dashboard />
    </main>
  );
}
